{
  "name": "figma-implement",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "format": "prettier . --check",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "vite": "^7.3.0"
  }
}
